# @package _global_
# Tuned for A10G (24 GB VRAM) — Modal / AWS / GCP

trainer:
  precision: bf16-mixed  # native bf16 on Ampere+

loader:
  batch_size: 256
  num_workers: 8
  prefetch_factor: 4

wandb:
  enabled: True
