defaults:
  - _self_

# Path to stage-2 checkpoint (HierarchicalLeWM object saved by train_hierarchical.py).
checkpoint: ???

device: cpu  # override with device=cuda when a GPU is available

seed: 42

world:
  env_name: swm/TwoRoom-v1
  num_envs: ${eval.num_eval}
  max_episode_steps: 500   # must be >= eval_budget (primitive steps)
  history_size: 3
  frame_skip: 1            # policy handles frameskip internally via action queue

dataset:
  name: tworoom
  keys_to_cache:
    - action
    - proprio

# Two-level CEM hyperparameters (mirror defaults from hierarchical_plan.plan)
plan:
  H_high: 1
  h_low: 10
  outer_samples: 512
  inner_samples: 256
  outer_iters: 5
  inner_iters: 30

# MPC loop config fed to swm.PlanConfig
plan_config:
  horizon: 1          # plan 1 high-level step per call (subgoal obtained internally)
  receding_horizon: 1  # replan every primitive action
  history_len: 1
  action_block: 1     # 1 primitive action per plan call; frameskip handled by world.frame_skip

eval:
  num_eval: 50
  goal_offset_steps: 25
  eval_budget: 250         # primitive steps; policy internally replans every frameskip=5 steps
  img_size: 224
  dataset_name: tworoom
  callables:
    - method: _set_state
      args:
        state:
          value: proprio
    - method: _set_goal_state
      args:
        goal_state:
          value: goal_proprio

output:
  filename: hierarchical_results.txt
